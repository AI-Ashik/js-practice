const fruits = ["orange","apple","mango","dragon"]

// it add a cerain given valuue to the array like push method (in the end)
// but in push method effect the main array and it return the new array length
// it dosent effect the main array
const result = fruits.concat("graps","lemon")
console.log(result)