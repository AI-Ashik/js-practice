const fruits = ["orange","apple","mango","dragon"]

// slice(start, end) - it sliced out the values that entered in the parameter(index)
// it dosent effect the main array

const result = fruits.slice(0,2)
console.log(result)