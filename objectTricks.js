const language = {
    language : "Javascript",
    created : 1995
}

const lan = Object.keys(language)
const lang = Object.values(language)
const langu = Object.entries(language)
console.log(lan)
console.log(lang)
console.log(langu)